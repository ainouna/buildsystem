--- a/configure.ac.org
+++ b/configure.ac
@@ -209,7 +209,7 @@
 getrandom_missing="no"
 
 if test "x$need_zlib" = "xyes"; then
-	PKG_CHECK_MODULES(ZLIB, [zlib], [], [zlib_missing="yes"])
+	PKG_CHECK_MODULES(ZLIB, [zlib1g], [], [zlib_missing="yes"])
 fi
 
 if test "x$need_selinux" = "xyes"; then

